[project]
name = "ai-engine"
version = "0.1.0"
description = "AI Content Factory AI Engine（V1 仅 system.echo 协议，Mock 模型）"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.32.0",
]
