[project]
name = "ai-engine"
version = "0.1.0"
description = "AI Content Factory AI 服务骨架（初始化阶段不含 Agent / 模型调用）"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.32.0",
]
