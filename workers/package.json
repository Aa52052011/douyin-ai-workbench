{
  "name": "workers",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "异步 Job Worker（BullMQ）。复用 backend 的 VideoGenerationService。",
  "scripts": {
    "start": "node ../apps/backend/dist/worker.js",
    "start:dev": "npm run start:worker:dev -w backend",
    "build": "npm run build -w backend"
  }
}
